import styles from '@/styles/Footer.module.css'

export default function Footer() {
    return (
        <footer className={styles.footer}>
            <p>Files are saved for one day on our servers and then deleted.</p>
        </footer>
    )
}